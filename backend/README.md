# Backend API

Place your backend service code in this folder.

Suggested structure:

- `src/` for server source files
- `package.json` for backend scripts and dependencies
- `.env` for environment variables (do not commit secrets)

Example run flow:

1. `cd backend`
2. `npm install`
3. `npm run dev`
